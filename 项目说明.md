# QQ AI 机器人 · 项目说明

> 一个把 GLM-5.3 大模型接入 QQ 的聊天机器人：NapCatQQ 协议端登录个人 QQ 号，Python 机器人程序负责对话与记忆，NapCat WebUI 内嵌管理控制台，桌面启动器 / 单文件 exe 一键启停。

## 一、系统架构

```
QQ 服务器
   ⇅
NapCatQQ（登录机器人QQ号，OneBot 11 协议端）
   ├─ 3001 正向WebSocket ──→ Python 机器人程序
   │                            ├─ 对话逻辑：群聊@触发 / 私聊回复 / 上下文记忆 / 人格
   │                            ├─ GLM-5.3（智谱开放平台，OpenAI 兼容协议）
   │                            └─ 内部管理API（127.0.0.1 随机端口，api.port 文件协调）
   └─ 6099 WebUI
        └─ 「🤖 QQ AI 机器人」插件页面（唯一管理入口，同源代理转发）
              ↑
       桌面启动器 / qq-ai-bot.exe（一键启动全部）
```

## 二、端口清单

| 端口 | 用途 | 归属 |
|---|---|---|
| **3001** | OneBot v11 正向 WebSocket（机器人收发 QQ 消息的唯一通道） | NapCat |
| **6099** | NapCat WebUI，含「QQ AI 机器人」管理控制台（**唯一管理入口**） | NapCat |
| 随机 | 机器人内部管理 API（系统分配临时端口，仅本机进程间通信，写入 `api.port`，退出即释放，**无需也无法直接访问**） | 机器人程序 |

> 曾使用的固定 8080 端口已移除：控制台页面与数据接口全部走 6099 同源代理，不再占用任何额外固定端口。

## 三、技术栈

| 层 | 技术 | 说明 |
|---|---|---|
| 协议端 | NapCatQQ v4.18（Shell 版） | 挂载官方 QQNT 客户端，提供 OneBot 11 接口与 WebUI |
| 机器人 | Python 3.11+，`websockets`、`openai`（兼容模式）、`tomllib`、`http.server` | 单进程异步架构；约千行代码，无重型框架 |
| 大模型 | 智谱 GLM-5.3（`glm-5.3`） | OpenAI 兼容协议；thinking 常开，`reasoning_effort=low` 保证聊天响应速度，可配置 |
| 管理控制台 | NapCat 插件体系（`router.page` 注册侧边栏页面 + `getNoAuth/postNoAuth` 同源 API） | 纯手写 HTML/JS + ESM 插件，零构建、零前端依赖 |
| 桌面启动器 | Python `tkinter`（标准库） | 状态灯 / 一键启动 / 进程日志 / 快捷方式 |
| 打包 | PyInstaller `--onefile --noconsole` | 产出单文件 `qq-ai-bot.exe`（约 23MB），目标机器无需 Python |
| 版本管理 | Git + GitHub | 敏感信息（API Key、QQ 号、token）全部排除在仓库外 |

## 四、需求达成对照表

| 需求 | 状态 | 实现方式 |
|---|---|---|
| AI 嵌入 QQ 聊天，使用 GLM-5.3 | ✅ | NapCat OneBot 11 + 智谱开放平台 OpenAI 兼容接口 |
| 群聊 @ 触发回复 | ✅ | CQ 码 `[CQ:at,qq=机器人号]` 识别；图片/表情等转为文字占位符 |
| 好友私聊自动回复 | ✅ | 可配白名单（空 = 回复所有人） |
| 多轮上下文记忆 | ✅ | 每群/每人独立会话，默认 20 轮裁剪，`data/sessions.json` 持久化重启不丢 |
| 自定义人格 | ✅ | 三通道：`config.toml`、聊天命令 `/性格`、控制台实时修改（`data/persona.json`） |
| 聊天命令 | ✅ | `/帮助` `/性格` `/清空记忆` `/状态`（群内 @ 使用） |
| 管理控制台（接口/Key/管理员/状态） | ✅ | WebUI 6099 侧边栏插件页：NapCat 接口测试与保存重连、API Key 测试与保存、管理员账号绑定、在线状态，全部即时生效并持久化 |
| 控制台不占用额外固定端口 | ✅ | 内部 API 用随机端口 + 插件同源代理（api.port 协调） |
| 一键启动桌面程序 | ✅ | `launcher.pyw` / `qq-ai-bot.exe`：一键启动、状态灯、日志、单独启停、桌面快捷方式 |
| 单文件可运行程序 | ✅ | PyInstaller 打包，双击即用、无需 Python 环境，自带插件自动部署 |
| 开源到 GitHub（无隐私泄露） | ✅ | `.gitignore` + 提交前全局扫描：API Key / QQ 号 / token 均不入库 |

## 五、文件结构

```
├── qq-ai-bot.exe        # 单文件程序（本地构建产物，不入库）
├── NapCat/              # NapCat 本体（shell + 启动脚本，登录缓存在内；不入库）
├── launcher.pyw         # 桌面启动器（源码）
├── bot.py               # 机器人主程序：WS客户端/事件分发/命令/内部API
├── glm_client.py        # GLM-5.3 调用封装
├── conversation.py      # 会话记忆管理
├── napcat-plugin/
│   └── napcat-plugin-qq-ai-bot/
│       ├── index.mjs    # NapCat 插件：页面注册 + 同源API转发
│       ├── package.json
│       └── webui/index.html  # 控制台页面
├── config.example.toml  # 配置模板（占位符）
├── config.toml          # 实际配置（含私密信息，不入库）
├── 项目说明.md           # 本文档
├── README.md            # 部署使用手册
└── data/                # 运行数据：日志/会话/人格/端口文件（不入库）
```

## 六、部署与运行

**方式 A：单文件 exe（推荐）**
1. 本机构建：`pyinstaller --onefile --noconsole --name qq-ai-bot --add-data "napcat-plugin;napcat-plugin" --exclude-module pandas --exclude-module numpy launcher.pyw`
2. 把 `qq-ai-bot.exe` 与填好的 `config.toml` 放同一目录
3. 双击 exe → 「🚀 一键启动」（自动：部署插件 → NapCat 快速登录 → 机器人 → 打开控制台）

**方式 B：源码运行**
```bash
pip install -r requirements.txt
python bot.py            # 仅机器人
python launcher.pyw      # 或带启动器
```

**前置条件**：已安装 QQ 与 NapCatQQ Shell（见 README 第二节）；智谱 API Key；`config.toml` 按 `config.example.toml` 填写。

## 七、分发说明

仓库不含任何密钥是有意设计：密钥是个人凭证，**每位使用者持自己的**（自己的智谱 API Key、自己设定的 NapCat 接口 token、自己的机器人 QQ 小号）。他人上手的详细步骤见 README「十一、给其他使用者」；其中 NapCat 接口 token 是自己设定的"暗号"（NapCat WebUI 网络配置里填写，与 config.toml 保持一致即可，可留空），WebUI 登录密钥在 NapCat 首次启动日志或其 `config/webui.json` 中查看。**分发时切勿携带自己的 config.toml 或密钥文件。**
